---
// Root layout for all pages
import '../styles/global.css';
import NavBar from '../components/NavBar';

interface Props {
  title?: string;
  /** Chat-like pages: full-height column, no container width, padding, or footer. */
  fullBleed?: boolean;
}

const { title = 'Comment Checker', fullBleed = false } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Comment Checker - Classify comments for hate speech and harmful content" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@500;600&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet" />
  <title>{title}</title>
</head>
<body class="font-sans antialiased bg-mistral-surface text-mistral-ink">
  <div class="min-h-screen flex flex-col">
    <!-- Navigation (client component: shows user state + logout) -->
    <NavBar client:only="react" />

    <!-- Main content -->
    {fullBleed ? (
      <main class="flex-1 flex flex-col min-h-0 w-full">
        <slot />
      </main>
    ) : (
      <main class="flex-1 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full">
        <slot />
      </main>
    )}

    {fullBleed ? null : (
      <footer class="bg-white border-t border-gray-200 py-6">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center text-gray-500 text-sm">
            &copy; {new Date().getFullYear()} Comment Checker. All rights reserved.
          </div>
        </div>
      </footer>
    )}
  </div>
</body>
</html>
